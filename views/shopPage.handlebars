<body>
    <main>
        <h1>{{shop.name}}</h1>
        {{#if loggedIn}}
            {{#if inBookmarks}}
                <form action="/shop/{{shop._id}}" method="POST">
                    <button type="submit" class="unbook">Un-bookmark</button>
                </form>
            {{else}}
                <form action="/shop/{{shop._id}}" method="POST">
                    <button type="submit" class="book">Bookmark</button>
                </form>
            {{/if}}
            <form action="/shop/{{shop._id}}/flagForm" method="GET">
                <button type="submit" class="flag">Flag Shop</button>
            </form>
        {{/if}}
        {{#with shop}}
        <h2>Details:</h2>
        <ul>
            <li><p>Address: {{address}}</p></li>
            <li><p>Website:  <a ={{website}}>{{website}}</a></p></li>
            <li><p>Phone Number: {{phoneNumber}}</p></li>
            <li><p>Average Rating: {{averageRating}}</p></li>
        </ul>
        {{/with}}
        <h2>Menu:</h2>
        <button onclick="toggleContent('toggledMenu')">Display Menu</button>
        <div id="toggledMenu" style="display: none;">
        <table>
            <tr>
                <th>Item</th>
                </th>Price</th>
                </th>Page Link</th>
            </tr>
        {{#each items}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.price}}</td>
                <td><a href="/item/{{this.id}}">Page</a></td>
            </tr>
        {{/each}}
        </table>
        </div>
        <h2>Reviews:</h2>
        <button onclick="toggleContent('toggledReviews')">Display Reviews</button>
        <div id="toggledReviews" style="display: none;">
        {{#each reviews}}
        <div>
            <h3>{{this.title}}</h3>
            <p>Rating: {{this.rating}}</p>
            <div>{{this.review}}</div>
            <p>{{this.reviewDate}}</p>
            {{#each comments}}
                <p>{{this.username}}</p>
                <div>
                    {{this.comment}}
                </div>
                <p>{{this.commentDate}}</p>
            {{/each}}
        </div>
        {{/each}}
        </div>  
        <script src="../public/clientside.js"></script>
        {{#if loggedIn}}
            <footer>
            <hr>
            <form action="/shop/{{shop._id}}/delete" method="POST">
                <button type="submit" class="delete">DELETE SHOP</button>
            </form>
            </footer>
        {{/if}}
    </main>
</body>